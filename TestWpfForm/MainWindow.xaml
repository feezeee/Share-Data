<Window x:Class="TestWpfForm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestWpfForm"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Background="#FF252526">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Width="{Binding ElementName=sd0, Path=Width}" HorizontalAlignment="Left">
            <local:ListCntrl x:Name="sd0">
            
            </local:ListCntrl>
        </StackPanel>
        <StackPanel Grid.Column="1" Width="{Binding ElementName=sd1, Path=Width}" HorizontalAlignment="Left">
            <local:ListCntrl x:Name="sd1">

            </local:ListCntrl>
        </StackPanel>
    </Grid>
</Window>
